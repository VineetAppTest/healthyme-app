# H9A.6F Profile Builder Time Selector Hotfix

Scope:

- Exercise Regime time now uses a time selector instead of free text.
- Supplement Regime time now uses a time selector instead of free text.
- Selected time continues to save as `HH:MM` text in the draft store.
- Saved draft time values are parsed back into the time selector when loaded.
- No SQL changes.
- No publish, activate, member-facing, or Flutter changes.
