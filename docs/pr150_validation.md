# PR validation

- `components/guards.py` syntax compiled locally before commit.
- Daily Log time controls use a stable four-column grid: Hour, colon, Minutes, AM/PM.
- Member session restoration gets one controlled retry after a browser refresh before redirecting to Login.
