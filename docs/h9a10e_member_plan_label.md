# H9A.10E Member Plan Label

Member-facing recommendation navigation should stay split into two clear options:

- Today's Journey: today's actionable slice from the active weekly recommendation.
- My Weekly Plan: the complete seven-day recommendation.

The route may remain `pages/37_Member_Plan.py`, but visible UI copy should use `My Weekly Plan` so members understand it as the overall plan rather than an internal member-plan object.

No SQL. No publish logic change. No Flutter change.
