streamlit[auth]==1.59.0
# Streamlit 1.59/1.60 does not yet pass Starlette 1.4's required
# GZipResponder thread_minimum_size argument. Keep rebuilds on the last
# verified compatible release until Streamlit ships the upstream fix.
starlette==1.3.1
pandas
openpyxl
supabase>=2.0.0
Authlib==1.6.6
requests>=2.31.0
extra-streamlit-components==0.1.81
# Country-to-IANA timezone resolution for member-local dates.
pytz>=2024.1
