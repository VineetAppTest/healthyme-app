# Temporary Gate 1 deployment only. Never commit real values.

AUTH_BRIDGE_PROVIDER = "supabaseoidc"
SUPABASE_URL = "https://<project-ref>.supabase.co"
SUPABASE_ANON_KEY = "<supabase-publishable-or-anon-key>"
SUPABASE_SERVICE_ROLE_KEY = "<server-only-service-role-key>"

[auth]
redirect_uri = "https://<native-role-bridge-app>.streamlit.app/oauth2callback"
cookie_secret = "<new-long-random-cookie-secret>"

[auth.supabaseoidc]
client_id = "<supabase-oauth-client-id>"
client_secret = "<supabase-oauth-client-secret>"
server_metadata_url = "https://<project-ref>.supabase.co/auth/v1/.well-known/openid-configuration"
client_kwargs = { scope = "openid email profile", prompt = "login" }
