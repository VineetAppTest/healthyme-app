{
  "build_name": "HealthyMe_Streamlit_H13A_Supabase_Refresh_Persistence_Role_Switch_Fix",
  "version": "v102.4B15S3H13A",
  "updated_on": "2026-07-20T08:00:00Z",
  "base_build": "v102.4B15S3H13 Supabase Auth Stability",
  "scope": [
    "Persists Supabase login across normal browser refresh using a random opaque SameSite cookie marker backed by a server-side session registry",
    "Keeps Supabase access and refresh tokens server-side and refreshes expiring access tokens without exposing credentials in URL or browser local storage",
    "Clears stale member-recovery hints so an explicit later Admin login routes by the authenticated HealthyMe role",
    "Retains the stable login-layout slots and single-step logout flow from H13",
    "Removes the retired v1 browser marker during logout and stale-session recovery"
  ],
  "excluded": [
    "No Flutter or Admin Lite code change",
    "No Supabase SQL or schema migration",
    "No assessment, report, recommendation, or member-data logic change",
    "No password, access token, or refresh token stored in browser cookies or local storage",
    "A Render process restart still requires a fresh sign-in because the secure token registry is process-local"
  ]
}
