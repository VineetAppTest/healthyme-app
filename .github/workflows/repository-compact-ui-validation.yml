name: Retired repository compact UI validation

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired by PR #338 after restoring the PR #327 repository baseline."
